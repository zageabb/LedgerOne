"""LedgerOne functional modules.

Each direct child package is discovered automatically by the module registry and
must expose a ``MANIFEST`` in ``manifest.py`` plus a ``register(app)`` function.
"""
